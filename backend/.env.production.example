# Backend Production Environment Variables
# Copy these and configure in your deployment platform (Railway, Render, Heroku)
# NEVER commit actual values to git!

# Flask Configuration
FLASK_ENV=production
FLASK_DEBUG=0
SECRET_KEY=your-random-secret-key-here-generate-with-secrets.token_urlsafe(32)
JWT_SECRET_KEY=your-jwt-secret-key-here-generate-with-secrets.token_urlsafe(32)

# Database (PostgreSQL)
# Format: postgresql://user:password@host:port/database
# Railway auto-generates this if PostgreSQL service is added
DATABASE_URL=postgresql://username:password@localhost:5432/bara_db

# CORS Configuration
# Set to your frontend URL(s) when deployed
CORS_ORIGINS=http://localhost:5173,https://bara-frontend.vercel.app

# AI Services
GEMINI_API_KEY=your-google-gemini-api-key-here
OPENAI_API_KEY=your-openai-api-key-optional

# Email Configuration (optional)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-specific-password
MAIL_USE_TLS=1

# Logging
LOG_LEVEL=INFO

{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "react",
  "env": {
    "VITE_API_BASE_URL": {
      "description": "Backend API base URL (e.g., https://bara-backend.railway.app/api)",
      "required": true
    }
  }
}
